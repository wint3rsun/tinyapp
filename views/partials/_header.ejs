<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav justify-content-left">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
      <div class="navbar-nav d-flex-row d-none d-md-none">
        <% if (user) { %>
          <form action="/logout" method="POST">
            <div class="from-group mb-2">
              <p>Logged In As: <%= user.email %></p>
              <button class="btn btn-outline-danger">Logout</button>
            </div>
          </form>
          <%} else { %>
            <a class="nav-item nav-link " href="/login">Login</a>
            <a class="nav-item nav-link p-2" href="/register">Register</a>
            <% } %>
      </div>
    </div>
    <div class="navbar-nav justify-content-right d-none d-md-block">
          <% if (user) { %>
            <form action="/logout" method="POST">
              <div class="from-group mb-2">
                <p>Logged In As: <%= user.email %></p>
                <button class="btn btn-outline-danger">Logout</button>
              </div>
            </form>
            <%} else { %>
              <a class="nav-item nav-link" href="/login">Login</a>
              <a class="nav-item nav-link" href="/register">Register</a>
      <% } %>
    </div>
  </nav>
</header>